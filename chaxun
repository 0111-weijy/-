create table student(sno char(10) primary key,
            sname char(10) not null,
            ssex char(2) not null,
            sage int not null,
            sdept char(10))
create table course(cno char(10) primary key,
            cname char(10),
            cpno char(10),
            ccredit(10))
create table sc(sno char(10),
          cno  char(10),
          grade char(10),
          primary key(sno,cno)
          foreign key(sno)references student(sno),
          primary key(cno) references course (cno))
          
alter table student add sloc char(10)
alter table student drop column sloc
alter table student sname char(20)

创建唯一索引学号
on student (sno)
create cluster index course
on coourse (cno)
变更索引学号重命名为学生的学号
下降指数学生的学号onstudent

select sno,sname
from student 
select sname,sno,sdept
from student
select*
from student
select sname,2014- sage as birthday
from student
select distinct sno
from sc
select sname,sage
from student
where sage<20
select sanme
from student
where sdept='cs'
select sno
from sc 
where grade<60
select sname,sdept,sage
from student
where sage between 20 and 23
select sname,ssex
from student
where sdept in ('cs','is','ma')
select sname,ssex
from student
where sdept not in ('cs','is','ma')
select *
from student
where sno='201215121'
select sno,sname,ssex
from student
像“刘%'

